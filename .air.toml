root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go", "env"]
  exclude_dir = ["tmp", "vendor", ".git", "docs"]
  delay = 200

[log]
  time = true

[misc]
  clean_on_exit = true
